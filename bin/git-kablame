#!/usr/bin/env ruby
#

usage = <<USAGE
Run git-kablame
Usage: git-kablame <directory>
USAGE

require 'fileutils'

targets = ARGV

if targets.nil? || targets.empty?
	puts usage
	exit(0)
end
require 'rubygems'
gem 'kablame'
require 'kablame'
GitKablame.new(targets).kablame